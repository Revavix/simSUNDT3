{
  "name": "simsundt3",
  "private": true,
  "version": "0.0.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "NODE_ENV=development concurrently 'npm run web:watch' 'sleep 1 && npm run electron:start'",
    "web:watch": "vite",
    "electron:start": "electron src",
    "build": "vite build && electron-builder"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.1.0",
    "concurrently": "^7.5.0",
    "electron": "^21.2.2",
    "electron-builder": "^23.6.0",
    "svelte": "^3.52.0",
    "vite": "^3.2.3"
  },
  "build": {
    "files": [
      "src/**/*"
    ]
  }
}
